# Test Cases — EPIC-12 — API de Integración

> Generado el 2026-07-02 desde `docs/issues/EPIC-12*.md` + Planilla CEPA (Anexo 4). Trazabilidad completa en [`../RTM.md`](../RTM.md) y `TestCases_RTM_2026-07-02.xlsx`.
>
> **Ejecución 2026-07-04**: 23/23 TCs ejecutados en ambiente local (13 Pass, 10 Fail). Detalle, evidencia y hallazgos en [`../execution-log/EPIC-12-run-2026-07-04.md`](../execution-log/EPIC-12-run-2026-07-04.md).

## CEPA-120

| TC ID | Categoría | Feature/Módulo | Descripción | Precondiciones | Pasos | Resultado esperado | Prioridad | Severidad | Fuente | Estado |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-CEPA-120-001 | Functional | Gateway de autenticación de la API | Verificar que un cliente externo con token JWT válido y vigente obtiene respuesta HTTP estándar (200/201) con cuerpo JSON conforme al contrato al invocar un endpoint bajo /api/v1. | API v1 desplegada en QA; cliente de integración registrado con token JWT firmado y vigente; existe al menos un paciente registrado (RUT 12.345.678-5). | 1. Obtener un token JWT válido para el cliente de integración. 2. Enviar GET a /api/v1/pacientes?rut=12.345.678-5 con header 'Authorization: Bearer <jwt>' y 'Accept: application/json'. 3. Inspeccionar el código HTTP, el header Content-Type y el cuerpo de la respuesta. | La API responde 200 OK, Content-Type application/json y cuerpo JSON conforme al esquema del contrato v1 (campos y tipos según OpenAPI). | P0 | Critical | AC:CA-1 | Pass |
| TC-CEPA-120-002 | Security | Gateway de autenticación de la API | Verificar que un cliente sin token o con JWT inválido/expirado recibe 401 Unauthorized con cuerpo JSON de error sin exponer datos del recurso. | API v1 desplegada en QA; endpoint protegido disponible (/api/v1/pacientes); se dispone de un JWT expirado y de un JWT con firma alterada. | 1. Enviar GET a /api/v1/pacientes sin header Authorization. 2. Enviar GET a /api/v1/pacientes con header 'Authorization: Bearer <jwt_expirado>'. 3. Enviar GET a /api/v1/pacientes con header 'Authorization: Bearer <jwt_firma_alterada>'. 4. Inspeccionar código HTTP y cuerpo de cada respuesta. | Las tres solicitudes responden 401 Unauthorized con cuerpo JSON de error uniforme; ninguna respuesta contiene datos de pacientes ni detalles internos del sistema. | P0 | Critical | AC:CA-2 | Pass |
| TC-CEPA-120-003 | Functional | Generador de especificación OpenAPI/Swagger | Verificar que Swagger UI / OpenAPI lista el 100% de los endpoints con esquemas de request/response, códigos de error y requisitos de autenticación. | API v1 desplegada en QA con documentación automática habilitada; inventario de endpoints implementados disponible (rutas del router FastAPI). | 1. Acceder en el navegador a la ruta de Swagger UI (/docs). 2. Descargar el documento OpenAPI (/openapi.json). 3. Comparar la lista de endpoints del documento contra el inventario de rutas implementadas. 4. Revisar para cada endpoint la presencia de esquemas de request/response, códigos de error documentados y el requisito de seguridad (bearer JWT). | Swagger UI carga correctamente; el documento OpenAPI es válido y contiene el 100% de los endpoints implementados, cada uno con esquemas de request/response, códigos de error y requisito de autenticación declarado (criterio OI3). | P0 | Major | AC:CA-3 | Fail |
| TC-CEPA-120-004 | Functional | Motor de rate limiting/throttling | Verificar que al superar la cuota de solicitudes por ventana de tiempo la API responde 429 Too Many Requests indicando el tiempo de reintento. | API v1 desplegada en QA; rate limiting configurado con cuota conocida (ej. N=100 solicitudes por ventana de 60 segundos) para el token de prueba. | 1. Obtener un token JWT válido para el cliente de prueba. 2. Enviar N solicitudes GET a /api/v1/pacientes dentro de la misma ventana de 60 segundos. 3. Enviar la solicitud N+1 dentro de la misma ventana. 4. Inspeccionar código HTTP, cuerpo y headers (Retry-After) de la solicitud N+1. 5. Esperar el fin de la ventana y enviar una solicitud adicional. | Las primeras N solicitudes responden 2xx; la solicitud N+1 responde 429 Too Many Requests con indicación del tiempo de reintento (header Retry-After o campo en el JSON de error); tras expirar la ventana el cliente vuelve a recibir 2xx. | P0 | Major | AC:CA-4 | Fail |
| TC-CEPA-120-005 | Regression | Enrutador de versiones de la API | Verificar que al publicar v2 con cambios incompatibles, v1 sigue operativa bajo su prefijo y los clientes existentes no se rompen. | Versiones /api/v1 y /api/v2 publicadas en QA, con v2 conteniendo al menos un cambio incompatible sobre un recurso común; suite de contrato de v1 disponible. | 1. Invocar el mismo recurso lógico (ej. Pacientes) en /api/v1/pacientes y en /api/v2/pacientes con un JWT válido. 2. Validar la respuesta de cada versión contra su respectivo esquema OpenAPI. 3. Ejecutar la suite de contrato de v1 completa tras el despliegue de v2. | Ambos prefijos responden según su propio contrato; la suite de contrato de v1 pasa sin cambios: ningún cliente de v1 se rompe por la publicación de v2. | P0 | Major | AC:CA-5 | Fail |
| TC-CEPA-120-006 | Business Rules | Manejador global de errores de la API | Verificar que los errores de la API devuelven códigos HTTP estándar (400, 401, 403, 404, 409, 422, 429, 500) con cuerpo JSON uniforme. | API v1 desplegada en QA; token JWT válido; token de rol sin permisos de escritura; esquema del cuerpo de error uniforme definido en el contrato. | 1. Enviar GET a /api/v1/pacientes/999999 (recurso inexistente) con JWT válido. 2. Enviar POST a /api/v1/pacientes con payload que incumple el esquema (ej. RUT vacío). 3. Enviar PATCH a un recurso con un JWT de rol sin permiso de escritura. 4. Enviar GET sin token. 5. Comparar el cuerpo JSON de cada respuesta de error contra el esquema uniforme (mismos campos: código, mensaje, detalle). | Cada escenario responde con su código estándar (404, 422, 403, 401 respectivamente) y todos los cuerpos de error comparten la misma estructura JSON uniforme definida en el contrato; ninguno expone trazas internas. | P0 | Major | BR:RN-3 | Fail |
| TC-CEPA-120-007 | Business Rules | Log de auditoría de integraciones | Verificar que toda operación de integración que crea o modifica datos queda registrada en el log de auditoría con quién/qué/cuándo, consistente con el RBAC. | API v1 desplegada en QA; token JWT válido asociado a un cliente identificable; acceso de lectura al log de auditoría. | 1. Enviar POST a /api/v1/pacientes con un paciente nuevo (RUT 12.345.678-5) usando el JWT del cliente de prueba. 2. Enviar PATCH al mismo recurso modificando un campo. 3. Consultar el log de auditoría filtrando por el cliente y la ventana de tiempo de las operaciones. 4. Verificar el contenido de cada entrada de auditoría. | El log de auditoría contiene una entrada por cada operación de creación y modificación, registrando la identidad del cliente/token (quién), el recurso y la acción (qué) y la fecha/hora (cuándo), consistente con el RBAC del sistema. | P0 | Critical | BR:RN-6 | Fail |

## CEPA-121

| TC ID | Categoría | Feature/Módulo | Descripción | Precondiciones | Pasos | Resultado esperado | Prioridad | Severidad | Fuente | Estado |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-CEPA-121-001 | Functional | Recurso API Pacientes | Verificar que el recurso Pacientes permite buscar por RUT, nombre o folio, devuelve los datos del paciente o 404 si no existe, y soporta CRUD según permisos. | API v1 desplegada en QA; JWT válido con permisos CRUD sobre Pacientes; paciente existente con RUT 12.345.678-5, nombre 'María Pérez Soto' y folio 2026-0001. | 1. Enviar GET a /api/v1/pacientes?rut=12.345.678-5 con JWT válido. 2. Enviar GET a /api/v1/pacientes?nombre=María Pérez. 3. Enviar GET a /api/v1/pacientes?folio=2026-0001. 4. Enviar GET a /api/v1/pacientes?folio=9999-9999 (folio inexistente). 5. Enviar POST creando un paciente nuevo, PATCH actualizándolo y DELETE eliminándolo con el mismo JWT. | Los pasos 1-3 devuelven 200 OK con los datos del mismo paciente; el paso 4 devuelve 404 Not Found con JSON de error; las operaciones CRUD del paso 5 responden 201/200/200-204 respectivamente conforme a los permisos del token. | P0 | Critical | AC:CA-1 | Fail |
| TC-CEPA-121-002 | Functional | Recurso API Ingresos | Verificar que el recurso Ingresos permite consultar y actualizar el estado de un ingreso, persistiendo la actualización con trazabilidad. | API v1 desplegada en QA; JWT válido con permiso de escritura sobre Ingresos; ingreso existente con folio 2026-0001 en estado 'Activo'. | 1. Enviar GET a /api/v1/ingresos/2026-0001 con JWT válido y registrar el estado devuelto. 2. Enviar PATCH a /api/v1/ingresos/2026-0001 con body {"estado": "Alta"}. 3. Enviar GET nuevamente al mismo ingreso. 4. Consultar el log de auditoría por la operación de actualización. | El GET inicial refleja el estado actual ('Activo'); el PATCH responde 200 OK; el GET posterior devuelve estado 'Alta' (persistido); el log de auditoría registra la actualización con quién/qué/cuándo. | P0 | Critical | AC:CA-2 | Fail |
| TC-CEPA-121-003 | Integration | Recurso API Fichas clínicas | Verificar que el recurso Fichas clínicas procesa ambos sentidos del intercambio con el sistema externo: recepción (push) y entrega (pull) de datos clínicos, confirmando con código HTTP estándar. | API v1 desplegada en QA; sistema externo de fichas clínicas integrado (o simulado) con JWT válido; paciente existente con folio 2026-0001. | 1. Enviar POST (push) a /api/v1/fichas-clinicas con un payload clínico JSON asociado al folio 2026-0001. 2. Registrar el código HTTP y el identificador devuelto. 3. Enviar GET (pull) a /api/v1/fichas-clinicas?folio=2026-0001. 4. Comparar los datos recuperados con los enviados en el push. | El push responde 201/200 confirmando la recepción; el pull responde 200 OK y devuelve los mismos datos clínicos enviados, demostrando el intercambio bidireccional (recibir y entregar). | P0 | Critical | AC:CA-3 | Pass |
| TC-CEPA-121-004 | Functional | Recurso API Licencias | Verificar que el recurso Licencias devuelve el historial de licencias y el total de días acumulados del folio, con el cálculo del módulo de Licencias Médicas (§7.7.3). | API v1 desplegada en QA; JWT válido; paciente con folio 2026-0001 con 3 licencias registradas (ej. 14, 10 y 7 días = 31 días acumulados). | 1. Enviar GET a /api/v1/licencias?folio=2026-0001 con JWT válido. 2. Contar las licencias devueltas en el historial. 3. Comparar el campo de días acumulados devuelto contra la suma esperada de días de las licencias del folio (31). | La API responde 200 OK con el historial completo (3 licencias) y el total de días acumulados correcto (31), consistente con el cálculo del módulo de Licencias Médicas. | P0 | Critical | AC:CA-4 | Pass |
| TC-CEPA-121-005 | Integration | Conector de integración SAM/SALUTEM | Verificar que al procesar datos clínicos provenientes de SAM/SALUTEM el CEPA solo recibe/consulta y persiste en su dominio, sin ejecutar ninguna operación de escritura sobre SALUTEM. | Integración SALUTEM activa en QA (o mock instrumentado que registre todas las llamadas recibidas); dataset de datos clínicos con origen SALUTEM disponible. | 1. Activar el registro/captura de tráfico saliente del CEPA hacia SALUTEM (proxy o mock instrumentado). 2. Enviar al CEPA un lote de datos clínicos con origen SALUTEM para el folio 2026-0001. 3. Consultar en el dominio CEPA que los datos quedaron persistidos localmente. 4. Revisar el registro del mock/proxy de SALUTEM buscando operaciones de escritura (POST/PUT/PATCH/DELETE) originadas por el CEPA. | Los datos quedan persistidos en el dominio CEPA; el registro de tráfico hacia SALUTEM no contiene ninguna operación de escritura originada por el CEPA (solo lecturas/consultas, si las hay), cumpliendo D12. | P0 | Critical | AC:CA-5 | Pass |
| TC-CEPA-121-006 | Business Rules | Validador de RUT del recurso Pacientes | Verificar que la búsqueda de pacientes por RUT valida el dígito verificador y rechaza RUTs con DV incorrecto. | API v1 desplegada en QA; JWT válido; paciente existente con RUT 12.345.678-5 (DV correcto). | 1. Enviar GET a /api/v1/pacientes?rut=12.345.678-5 (DV correcto) con JWT válido. 2. Enviar GET a /api/v1/pacientes?rut=12.345.678-9 (DV incorrecto). 3. Enviar GET a /api/v1/pacientes?rut=12345678K (formato/DV inválido). 4. Inspeccionar códigos HTTP y cuerpos de error de los pasos 2 y 3. | El paso 1 responde 200 OK con el paciente; los pasos 2 y 3 responden 422 (o 400) con JSON de error uniforme indicando RUT inválido por dígito verificador, sin consultar el recurso. | P0 | Major | BR:RN-3 | Fail |
| TC-CEPA-121-007 | Business Rules | Enrutador de recursos de la API v1 | Verificar que la API v1 P0 solo expone los recursos definidos en §8.2 (Pacientes, Ingresos, Fichas clínicas, Licencias) y no publica endpoints adicionales ni los recursos P1/P2 (Fármacos, Controles, Reportes). | API v1 desplegada en QA; documento OpenAPI accesible; JWT válido. | 1. Descargar el documento OpenAPI de la v1. 2. Listar todos los paths publicados. 3. Verificar que cada path corresponde a Pacientes, Ingresos, Fichas clínicas, Licencias o a rutas de salud/documentación. 4. Enviar GET a /api/v1/farmacos, /api/v1/controles y /api/v1/reportes con JWT válido. | El OpenAPI solo publica los recursos del alcance P0 de §8.2; las solicitudes a Fármacos, Controles y Reportes responden 404 Not Found (no existen en v1 P0). | P0 | Medium | BR:RN-6 | Fail |
| TC-CEPA-121-008 | Business Rules | Log de auditoría de integraciones | Verificar que toda operación de escritura/recepción sobre los recursos clínicos queda registrada en el log de auditoría, consistente con OI1 y RBAC. | API v1 desplegada en QA; JWT válido con permisos de escritura; acceso de lectura al log de auditoría. | 1. Enviar POST a /api/v1/pacientes creando un paciente (RUT 12.345.678-5). 2. Enviar PATCH a /api/v1/ingresos/2026-0001 cambiando el estado. 3. Enviar POST (push) a /api/v1/fichas-clinicas con un payload clínico. 4. Consultar el log de auditoría filtrando por el token del cliente y la ventana de tiempo de las tres operaciones. | El log de auditoría contiene una entrada por cada una de las tres operaciones de escritura/recepción, con identidad del cliente, recurso/acción y fecha/hora, consistente con el RBAC. | P0 | Critical | BR:RN-7 | Pass |
| TC-CEPA-121-009 | Security | Autorización RBAC de la API | Verificar que un token sin permiso de escritura sobre el recurso Ingresos recibe 403 Forbidden al intentar actualizar el estado, sin que se aplique ningún cambio. | API v1 desplegada en QA; JWT válido de un rol sin permiso de escritura sobre Ingresos (ej. Auditor, solo lectura); ingreso existente folio 2026-0001 en estado 'Activo'. | 1. Enviar PATCH a /api/v1/ingresos/2026-0001 con body {"estado": "Alta"} usando el JWT del rol sin escritura. 2. Inspeccionar el código HTTP y el cuerpo de la respuesta. 3. Enviar GET al mismo ingreso con un JWT con permiso de lectura y verificar el estado. | El PATCH responde 403 Forbidden con JSON de error; el ingreso conserva su estado 'Activo' sin ningún cambio persistido. | P0 | Critical | TC:TC-121-06 | Pass |

## CEPA-122

| TC ID | Categoría | Feature/Módulo | Descripción | Precondiciones | Pasos | Resultado esperado | Prioridad | Severidad | Fuente | Estado |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-CEPA-122-001 | Integration | Receptor de licencias electrónicas IMED | Verificar que el CEPA recibe y persiste una licencia médica electrónica enviada por un cliente IMED autenticado, vinculada al paciente/folio, confirmando con código HTTP estándar. | Endpoints IMED desplegados en QA; cliente IMED con JWT válido y scope IMED; paciente existente con RUT 12.345.678-5 y folio 2026-0001. | 1. Enviar POST a /api/v1/imed/licencias con payload IMED de licencia médica electrónica (RUT 12.345.678-5, folio 2026-0001, 14 días, fecha inicio 01-06-2026) usando el JWT del cliente IMED. 2. Registrar el código HTTP y el identificador devuelto. 3. Consultar el recurso Licencias del folio 2026-0001 y verificar que la licencia figura en el historial vinculada al paciente/folio. | La API confirma con 201/200; la licencia queda persistida en el CEPA vinculada al paciente/folio 2026-0001 y visible en el historial de licencias. | P2 | Major | AC:CA-1 | Pass |
| TC-CEPA-122-002 | Integration | Receptor de recetas electrónicas IMED | Verificar que el CEPA recibe y persiste una receta electrónica enviada por un cliente IMED autenticado, vinculada al folio del paciente. | Endpoints IMED desplegados en QA; cliente IMED con JWT válido y scope IMED; paciente existente con folio 2026-0001. | 1. Enviar POST a /api/v1/imed/recetas con payload IMED de receta electrónica (folio 2026-0001, medicamento, dosis, fecha de emisión 15-06-2026) usando el JWT del cliente IMED. 2. Registrar el código HTTP devuelto. 3. Consultar en el CEPA las recetas del folio 2026-0001 y verificar la vinculación. | La API confirma con 201/200; la receta queda persistida en el CEPA vinculada al folio 2026-0001 del paciente. | P2 | Major | AC:CA-2 | Pass |
| TC-CEPA-122-003 | Integration | Arquitectura del contrato API v1 | Verificar que la integración IMED puede diferirse y habilitarse a futuro sin rediseño del contrato base v1 (feature toggle / extensión del contrato). | API v1 desplegada en QA con la integración IMED deshabilitada/diferida; documento OpenAPI v1 base disponible; suite de contrato v1 en verde. | 1. Con IMED deshabilitado, ejecutar la suite de contrato de la API v1 base. 2. Habilitar la integración IMED (configuración/feature toggle) sin modificar los endpoints existentes. 3. Reejecutar la suite de contrato v1 base. 4. Comparar el documento OpenAPI antes y después de habilitar IMED. | La suite de contrato v1 pasa en ambos estados; habilitar IMED solo agrega endpoints nuevos bajo el contrato v1 sin modificar ni romper los existentes (sin rediseño del contrato base). | P2 | Medium | AC:CA-3 | Pass |
| TC-CEPA-122-004 | Business Rules | Log de auditoría de integraciones | Verificar que los datos recibidos desde IMED quedan vinculados al folio del paciente y registrados en el log de auditoría. | Endpoints IMED desplegados en QA; cliente IMED con JWT válido; paciente con folio 2026-0001; acceso de lectura al log de auditoría. | 1. Enviar POST a /api/v1/imed/licencias con una licencia para el folio 2026-0001. 2. Enviar POST a /api/v1/imed/recetas con una receta para el mismo folio. 3. Consultar el log de auditoría filtrando por el cliente IMED y la ventana de tiempo. 4. Verificar la vinculación al folio en cada entrada. | El log de auditoría registra ambas recepciones con identidad del cliente IMED, tipo de dato recibido, folio vinculado (2026-0001) y fecha/hora. | P2 | Major | BR:RN-3 | Fail |
| TC-CEPA-122-005 | Business Rules | Generador de especificación OpenAPI/Swagger | Verificar que los endpoints IMED reutilizan los principios de §8.1 (versionado /api/v1, JWT, JSON, rate limiting) y figuran documentados en la spec OpenAPI del contrato v1, sin contrato paralelo. | Endpoints IMED publicados en QA; documento OpenAPI v1 accesible; rate limiting configurado. | 1. Descargar el documento OpenAPI de la v1. 2. Verificar que los endpoints IMED (licencias y recetas) figuran bajo el prefijo /api/v1 con esquemas y requisito de autenticación JWT. 3. Enviar una solicitud IMED sin token y verificar el rechazo. 4. Enviar solicitudes IMED sobre la cuota de rate limiting y verificar la respuesta. | Los endpoints IMED están documentados en la misma spec OpenAPI del contrato v1 (sin contrato paralelo), bajo /api/v1, exigen JWT (401 sin token) y aplican rate limiting (429 al exceder la cuota). | P2 | Medium | BR:RN-4 | Pass |
| TC-CEPA-122-006 | Functional | Validador de payloads IMED | Verificar que un payload IMED mal formado (sin campos obligatorios) es rechazado con 422 sin persistir ningún dato. | Endpoints IMED desplegados en QA; cliente IMED con JWT válido; conteo previo de licencias del folio 2026-0001 conocido. | 1. Enviar POST a /api/v1/imed/licencias con un JSON al que le faltan campos obligatorios (ej. sin RUT ni días de licencia). 2. Inspeccionar el código HTTP y el cuerpo de error. 3. Consultar las licencias del folio 2026-0001 y comparar el conteo con el previo. | La API responde 422 Unprocessable Entity con JSON de error uniforme detallando los campos faltantes; no se persiste ningún registro (el conteo de licencias no cambia). | P2 | Major | TC:TC-122-03 | Pass |
| TC-CEPA-122-007 | Security | Autorización RBAC de la API | Verificar que un token sin scope IMED recibe 403 Forbidden al intentar enviar una receta electrónica a los endpoints IMED. | Endpoints IMED desplegados en QA; JWT válido de un cliente sin scope/permiso IMED; payload de receta electrónica válido para el folio 2026-0001. | 1. Enviar POST a /api/v1/imed/recetas con el payload de receta usando el JWT sin scope IMED. 2. Inspeccionar el código HTTP y el cuerpo de la respuesta. 3. Consultar en el CEPA las recetas del folio 2026-0001 y verificar que no se agregó ninguna. | La API responde 403 Forbidden con JSON de error; ninguna receta queda persistida. | P2 | Major | TC:TC-122-04 | Pass |
